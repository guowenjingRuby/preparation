修饰成员变量：
public:
通过创建对象访问。所有地方都可以访问。
不通过创建对象，子类（同包不同包）和类体内

protected:
通过创建对象访问。同包下可以访问，子类可以访问（同包），子类可以访问（不同包），类体内
不通过创建对象，子类（同包/不同包）和类体内。

default:
通过创建对象可以访问：同包下可以访问，子类可以访问（同包下）,类体内
不通过创建对象，子类（同包）和类体内。
private:
只能本类可以访问

修饰方法：
public:
不通过创建对象：所有的地方都能访问。


